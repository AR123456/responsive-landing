
//  * **Word**: Used to create an object representing the current word the user is attempting to guess. This should contain word specific logic and data.
function Words() {
    console.log( "You are linked to the words.js file  ");
    // currentWord = wordList[Math.floor(Math.random()* wordList.length)];//random generates word from the list from the options array. ???
    // this function will used to get the word needed by the  letters function 
}

module.exports = Words;